import { v2 } from "../v2";

const node_ship_particles: [number, number, number, number][] = [];

export function createShipNode()
{

}

function renderShipNode(nodeId: number, now: number, delta: number): void
{

}